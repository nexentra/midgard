with-expecter: True
filename: "mock_{{.InterfaceName}}.go"
dir: "{{.InterfaceDir}}"
mockname: "Mock{{.InterfaceName}}"
outpkg: "{{.PackageName}}"
inpackage: True
packages:
  github.com/nexentra/midgard/handlers:
    interfaces:
      PersonalAccessTokenService:
      TaskService:
      UserService:
  github.com/nexentra/midgard/services:
    interfaces:
      PersonalAccessTokenMapper:
      TaskMapper:
      UserMapper:
